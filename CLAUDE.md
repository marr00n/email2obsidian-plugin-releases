# CLAUDE.md

Guidance for Claude Code when working in this repository.

## Session naming

All Claude sessions for this project must have the prefix `E2OP:` added to their name (e.g. `E2OP: experimentation`). This distinguishes this project's sessions from sessions belonging to other projects run in parallel. Rename the session at the start of work if the prefix is missing.
